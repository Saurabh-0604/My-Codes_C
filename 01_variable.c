#include <stdio.h>

int main()
{
    float a = 2.0, b = 3.0;
    float k = 2.0/3.0;
    //%d is called faormat specifier
    //%d is for int, %f is for float and %c is for char
    printf("the value of a/b is %f",k);
    return 0;
}